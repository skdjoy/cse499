You need to install Node JS first. Once installed open the project folder in terminal and type:
  npm install
The click enter. It will autometically install all the node package with their dependencies.

After it is done open type:
  node server.js
The server will start.

If the server starts correctly open a chrome or mozilla browser and type the url:
  localhost:8000
The website should start correctly.
